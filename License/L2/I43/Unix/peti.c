#include<stdlib.h>
#include<stdio.h>
int main(int argc, char**argv, char**w)
{
  int x = atoi (argv[1]);
  int i = 0;
  i = 0; while(argv[i]) puts(argv[i++]);
  i = 0; while(w[i]) puts(w[i++]);
  i=0;13167
  while(i != 1) i+=2;
  return x;
}